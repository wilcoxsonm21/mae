# Sweep configuration for latent dimension (quantitative)
# This sweeps over different latent space dimensions to find the optimal size

parameter: "model.params.latent_dim"
values: [16, 32, 64, 128, 256]
