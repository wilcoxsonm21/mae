# Sweep configuration for MAE mask ratio (quantitative)
# This sweeps over different masking ratios for masked autoencoders
# Note: Only use this with mlp_mae model type

parameter: "model.params.mask_ratio"
values: [0.3, 0.4, 0.5, 0.6, 0.7]
